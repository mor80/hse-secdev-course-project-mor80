version: "3.9"

services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
      target: runtime
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
